# ============================================================
# Piano Playing Level Classifier — Python Dependencies
# ============================================================
# Environment  : tugas-akhir
# Python       : 3.10.20
#
# Install (with the environment active):
#     pip install -r requirements.txt
# ============================================================

# ── Web server / REST API (flask_api.py) ────────────────────
Flask==3.1.3                 # web framework: /predict, /video/<name>, /health
Werkzeug==3.1.8              # secure_filename() + WSGI layer used by Flask

# ── Computer vision & hand tracking ─────────────────────────
opencv-python==4.10.0.84     # read/write video frames, drawing overlays
mediapipe==0.10.14           # 21-point hand landmark detection
protobuf==4.25.3             # required by mediapipe

# ── Numerical processing ────────────────────────────────────
numpy==1.26.4                # feature vectors, angles, distances, averaging

# ── Deep learning (LSTM ensemble, 5 folds .h5) ──────────────
tensorflow==2.15.0           # loads and runs best_model_fold_1..5.h5
keras==2.15.0                # Keras backend bundled with TensorFlow 2.15

# ── Video encoding (H.264 output + audio muxing) ────────────
imageio==2.37.3              # writes the final browser-playable MP4
imageio-ffmpeg==0.6.0        # bundled ffmpeg binary (no separate install needed)

# ── Note about the web interface ────────────────────────────
# index.php runs on Apache/PHP (XAMPP) and needs no Python package.
# It only requires the PHP cURL extension (php_curl), which is
# enabled by default in XAMPP.
